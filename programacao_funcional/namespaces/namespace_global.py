# Esta variável está no escopo "global" (modular)
a = 5

# Imprime quais são as globals que temos no nosso escopo
# Ex: __builtins__, que contem as funções padrões que podemos usar por padrão
print(globals())